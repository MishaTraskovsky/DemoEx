<Page x:Class="NBAManagement.WelcomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NBAManagement"
      mc:Ignorable="d"
      Title="WelcomePage" Height="129.188" Width="224.365">

    <Grid>
        <Border BorderBrush="Gray" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center" Height="126" Margin="0,0,0,3" Width="224">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                <Button 
                Background="#6995c2"
                Foreground="White"
                Content="Visitor"
                FontSize="20"
                Margin="40"
                HorizontalAlignment="Left"
                VerticalAlignment="Center" Click="Button_Click"/>

                <Button 
                Background="#6995c2"
                Foreground="White"
                Content="Admin"
                FontSize="20"
                Margin="40"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
